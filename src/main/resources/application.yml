server:
  port: 9000
  
spring: 

 
  datasource:
    url: jdbc:mysql://localhost:3306/dbloan?useSSL=false
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
       # format_sql: true
